<!DOCTYPE html>
<html>
  <head>
    <title>E-commerce</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

  </head>

  <body class="bg-gray-100 text-gray-800 font-sans">
    <!-- Header -->
    <header class="bg-white shadow-md">
      <div class="container mx-auto flex justify-between items-center p-4">
        <h1 class="text-2xl font-bold">
          <%= link_to "E-commerce", root_path, class: "text-blue-600 hover:text-blue-800" %>
        </h1>
        <nav class="space-x-4">
          <% if user_signed_in? %>
            <div class="flex items-center">
              <span class="font-medium mr-4">Olá, <%= current_user.email %></span>
              <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "text-red-500 hover:text-red-700 bg-transparent border-0 p-0 cursor-pointer" %>
            </div>
          <% else %>
            <%= link_to "Entrar", new_user_session_path, class: "text-blue-500 hover:text-blue-700" %>
            <%= link_to "Registrar", new_user_registration_path, class: "text-green-500 hover:text-green-700" %>
          <% end %>
          <%= link_to "Carrinho", orders_path, class: "text-gray-700 hover:text-gray-900" %>
        </nav>
      </div>
    </header>

    <!-- Main content -->
    <main class="container mx-auto mt-6">
      <%= yield %>
    </main>

    <footer class="bg-white shadow-inner mt-10 p-4 text-center text-gray-500">
      &copy; <%= Time.now.year %> E-commerce 
    </footer>
  </body>
</html>
